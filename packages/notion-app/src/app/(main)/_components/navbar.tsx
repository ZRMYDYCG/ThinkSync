"use client";

interface NavbarProps {
  isCollapsed: boolean;
  onResizeWidth: () => void;
}

const Navbar = ({ isCollapsed, onResizeWidth }: NavbarProps) => {
  return <>Navbar</>;
};

export default Navbar;
