pre-commit:
  parallel: true
  commands:
    fmt:
      glob: '*.{js,jsx,ts,tsx,json,jsonc,css,scss,md,mdx,yml,yaml,html}'
      run: bun oxfmt --write {staged_files}
      stage_fixed: true
    lint:
      glob: '*.{js,jsx,ts,tsx}'
      run: bun oxlint {staged_files}

commit-msg:
  commands:
    'lint commit message':
      run: bun commitlint --edit {1}
